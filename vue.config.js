module.exports = {
    runtimeCompiler: true,
    publicPath: '/',
    // publicPath:
    // process.env.NODE_ENV === "production" ? "/" : "/",

    // publicPath: process.env.NODE_ENV === 'production'
    //       ? '/dist/'
    //       : '/'
      
  }
  